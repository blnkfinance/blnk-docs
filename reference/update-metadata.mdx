---
title: "Update metadata"
description: "How to update metadata information in your data."
api: "POST /{id}/metadata"
noindex: true
"og:title": "Update Metadata • Blnk API Reference"
---

import NeedHelp from "/snippets/need-help.mdx";

### Authorization
Basic authentication header of the form — `Basic <encoded-value>`, where `<encoded-value>` is the base64-encoded string of your `username:password`.

### Body
<ParamField body="meta_data" type="object">
  Metadata information to be added or updated.
</ParamField>

<RequestExample>
```json
curl --request POST \
  --url http://localhost:5001/{id}/metadata \
  --header 'Authorization: Basic <encoded-value>' \
  --header 'Content-Type: application/json' \
  --data '{
    "meta_data": {
      "project_owner": "Acme LLC",
      "update_status": "Approved"
    }
  }'
```
</RequestExample>

<ResponseExample>
```json 201
{
  "meta_data": {
    "project_owner": "Acme LLC",
    "description": "Approved"
  }
}
```
</ResponseExample>

<NeedHelp />