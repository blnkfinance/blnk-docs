---
title: "Balance snapshots"
description: "How to capture a balance snapshot."
api: "POST /balance-snapshots"
noindex: true
"og:title": "Balance Snapshots • Blnk API Reference"
---

import NeedHelp from "/snippets/need-help.mdx";

### Authorization
Basic authentication header of the form — `Basic <encoded-value>`, where `<encoded-value>` is the base64-encoded string of your `username:password`.

<RequestExample>
```json
curl --request POST \
  --url http://localhost:5001/balance-snapshots \
  --header 'Authorization: Basic <encoded-value>' \
  --header 'Content-Type: application/json' \
```
</RequestExample>

<NeedHelp />