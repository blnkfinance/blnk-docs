---
title: "Tokenize one field"
description: "How to tokenize a single field in Identities."
api: "POST /identities/{identity_id}/tokenize/{field}"
noindex: true
"og:title": "Tokenize One Field • Blnk API Reference"
---

import NeedHelp from "/snippets/need-help.mdx";

### Authorization
Basic authentication header of the form — `Basic <encoded-value>`, where `<encoded-value>` is the base64-encoded string of your `username:password`.

### Path
<ParamField path="identity_id" type="string">
  Specifies the unique ID of the identity being retrieved
</ParamField>
<ParamField path="field" type="string">
  Specifies the field to be tokenized
</ParamField>

<ResponseExample>
```json 201
{
    "message": "Field tokenized successfully"
}
```
</ResponseExample>

<NeedHelp />