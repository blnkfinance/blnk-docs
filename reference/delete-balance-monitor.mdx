---
title: "Delete balance monitor"
description: "Remove a balance monitor from your ledger."
api: "DELETE /balance-monitors/{monitor_id}"
noindex: true
"og:title": "Delete Balance Monitor • Blnk API Reference"
---

import NeedHelp from "/snippets/need-help.mdx";

import Key from "/snippets/key.mdx";

<Key />

### Path
<ParamField path="monitor_id" type="string" required>
  The unique ID of the balance monitor to delete.
</ParamField>

<RequestExample>
```bash
curl -X DELETE "http://localhost:5001/balance-monitors/{monitor_id}" \
  -H "X-blnk-key: <api-key>"
```
</RequestExample>

<ResponseExample>
```json 200
{
  "message": "BalanceMonitor deleted successfully"
}
```

```json 404
{
  "error": "Balance monitor not found",
  "error_detail": {
    "code": "BAL_MONITOR_NOT_FOUND",
    "message": "Balance monitor not found",
    "details": {}
  }
}
```
</ResponseExample>

<NeedHelp />
