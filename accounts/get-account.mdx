---
title: "Get Account - Blnk API Reference"
description: "Learn how to retrieve account information in Blnk - get specific accounts by ID or list all accounts with comprehensive API documentation."
api: "GET /accounts/{account_id} | GET /accounts"
"og:title": "Get Account • Blnk API Reference"
---

import NeedHelp from "/snippets/need-help.mdx";

import Key from "/snippets/key.mdx";

<Warning>
**Deprecated Feature**: The Accounts API has been deprecated in favor of using Balances. We recommend using the [Balances API](/balances/introduction) for all new implementations.
</Warning>

<Key />

### Path
<ParamField path="account_id" type="string" required>
  The unique identifier of the account to retrieve.
</ParamField>

### Query
<ParamField query="include" type="array">
  Comma-separated list of related entities to include in the response. Valid values: `balance`, `identity`, `ledger`.

  ```
  /accounts/{account_id}?include=balance,identity,ledger
  ```
</ParamField>

### Response
<ResponseField name="account_id" type="string">
  Unique identifier for the account.
</ResponseField>
<ResponseField name="name" type="string">
  The account name.
</ResponseField>
<ResponseField name="number" type="string">
  The account number.
</ResponseField>
<ResponseField name="bank_name" type="string">
  The name of the bank.
</ResponseField>
<ResponseField name="currency" type="string">
  The account currency.
</ResponseField>
<ResponseField name="balance_id" type="string">
  The ID of the linked balance.
</ResponseField>
<ResponseField name="identity_id" type="string">
  The ID of the linked identity.
</ResponseField>
<ResponseField name="ledger_id" type="string">
  The ID of the linked ledger.
</ResponseField>
<ResponseField name="created_at" type="timestamp">
  ISO 8601 formatted timestamp of when the account was created.
</ResponseField>
<ResponseField name="meta_data" type="object">
  Custom metadata associated with the account.
</ResponseField>

<RequestExample>
```bash
curl --request GET \
  --url http://localhost:5001/accounts/acc_123456789 \
  --header 'X-blnk-key: <api-key>'
```
</RequestExample>

<ResponseExample>
```json 200
{
  "account_id": "acc_123456789",
  "name": "John Doe",
  "number": "1234567890",
  "bank_name": "Blnk Bank",
  "currency": "USD",
  "balance_id": "bal_123456789",
  "identity_id": "id_123456789",
  "ledger_id": "led_123456789",
  "created_at": "2024-01-15T10:30:00Z",
  "meta_data": {
    "account_type": "checking",
    "branch_code": "001"
  }
}
```
</ResponseExample>

### Navigation

<CardGroup cols={3}>
<Card title="Introduction" icon="book-user" href="/accounts/introduction">
  Overview and key concepts
</Card>

<Card title="Create Account" icon="code" href="/accounts/create-account">
  Create new accounts
</Card>



<Card title="Account Generation" icon="code" href="/accounts/account-number-generation">
  Auto-generate details
</Card>
</CardGroup>

---

<NeedHelp /> 