---
title: "Balance from Source"
sidebarTitle: "Balance from source"
icon: "database"
description: "Compute balance directly from transactions instead of using the default running balance."
"og:title": "Balance from Source • Blnk Developer Documentation"
"og:description": "Compute balances directly from transactions in Blnk. Bypass balance snapshots and reconstruct balances from transaction history for accurate financial reporting."
---

import NeedHelp from "/snippets/need-help.mdx";
import { RelatedTopics } from "/snippets/related-topics.jsx";

<Info>Available in version 0.10.1 and later.</Info>

The [Balance from Source](/reference/balance-from-source) endpoint allows you to compute a balance directly from its transactions instead of using the default running balance. 

This is useful when you need to verify balance accuracy, perform audits, or ensure consistency by calculating balances independently from the stored balance values.


<CodeGroup>
```bash cURL wrap
curl -X GET "http://YOUR_BLNK_INSTANCE_URL/balances/{balance_id}?from_source=true" \
  -H "X-blnk-key: <api-key>"
```

```typescript TypeScript wrap
const response = await blnk.LedgerBalances.get(
  'bln_5ce86029-3c2e-4e2a-aae2-7fb931ca4c4f',
  {
    from_source: true,
  },
);
```

```go Go wrap
at, _ := time.Parse(time.RFC3339, "2024-04-22T15:28:03+00:00")
balance, resp, err := client.LedgerBalance.GetHistorical(
    "bln_5ce86029-3c2e-4e2a-aae2-7fb931ca4c4f",
    at,
    true,
)
```

```python Python wrap
response = blnk.ledger_balances.get(
  "bln_5ce86029-3c2e-4e2a-aae2-7fb931ca4c4f",
  {
    "from_source": True,
  },
)
```

```java Java wrap
ApiResponse<JsonNode> response = blnk.ledgerBalances().get(
    "bln_5ce86029-3c2e-4e2a-aae2-7fb931ca4c4f",
    GetBalanceRequest.create().fromSource(true));
```
</CodeGroup>

```json Response
{
  "balance": {
    "balance": 9620000,
    "balance_id": "bin_be16c4a1-b5a6-4b64-a733-de2f6b24813d",
    "credit_balance": 9620000,
    "currency": "USD",
    "debit_balance": 0,
    "ledger_id": ""
  }
}
```

---

## How it works

When you call the balance from source endpoint, Blnk reconstructs the balance by:

1. **Getting all applied transactions:** Blnk retrieves all applied transactions for the balance and aggregates them together. As a result, it may take longer to process for balances with extensive transaction history.

2. **Calculating credits and debits:** It aggregates all credit transactions (money moving into the balance) and debit transactions (money moving out of the balance) to compute the total credit balance, debit balance, and net balance.

3. **Returning the computed balance:** The final computed balance, reflecting the exact state based on transaction history, is returned in the response.

<Card title="Historical balances" icon="clock" href="/balances/historical-balances">
  Balances at specific past timestamps.
</Card>

<Card title="Balance snapshots" icon="camera" href="/balances/balance-snapshots">
  Point-in-time copies of running balances.
</Card>

---

<NeedHelp />

<RelatedTopics
  items={[
    { title: "Balances overview", href: "/balances/introduction" },
    { title: "Balance reconstruction", href: "/advanced/balance-reconstruction" },
    { title: "Balance from source", href: "/reference/balance-from-source" },
  ]}
/>
