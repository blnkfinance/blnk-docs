---
title: "List balances"
api: "GET /data/balances"
description: "List all balances from your Blnk Core instance with pagination support."
---

import NeedHelp from "/cloud/snippets/need-help.mdx";

Retrieve a paginated list of balances from your Blnk Core instance.

<Note>
For in-depth information about Blnk's ledger, balance, transaction, and identity APIs, see the [Blnk Core docs](https://docs.blnkfinance.com).
</Note>

---

### Request structure

<RequestExample>
```bash cURL
curl -X GET "https://api.cloud.blnkfinance.com/data/balances?instance_id=YOUR_INSTANCE_ID&page=1&pageSize=20" \
  -H "Authorization: Bearer blnk_at_YOUR_ACCESS_TOKEN"
```
</RequestExample>

<ParamField header="Authorization" type="string" required>
Bearer token for authentication. Format: `Bearer <access_token>`. Your OAuth key must include `data:read` scope (or `*`).
</ParamField>

---

### Query parameters

<ParamField query="instance_id" type="string" required>
The Blnk Core instance ID to read from. Find this in Blnk Cloud under Instances.
</ParamField>

<ParamField query="page" type="integer">
Page number to retrieve. Defaults to `1`.
</ParamField>

<ParamField query="pageSize" type="integer">
Number of results per page. Defaults to `20` for balances.
</ParamField>

---

### Response structure

<ResponseExample>
```json Success
{
  "data": [
    {
      "balance_id": "bln_5ce86029-3c2e-4e2a-aae2-7fb931ca4c4f",
      "ledger_id": "ldg_073f7ffe-9dfd-42ce-aa50-d1dca1788adc",
      "currency": "USD",
      "balance": 100.50,
      "created_at": "2024-11-26T08:36:36.238244338Z"
    }
  ],
  "total": 42
}
```
</ResponseExample>

<ResponseField name="data" type="array" required>
Array of balance objects.
</ResponseField>

<ResponseField name="total" type="integer" required>
Total number of balances available.
</ResponseField>

<Note>
Balances may also include a `"stats"` field in the response for aggregate views.
</Note>

<Expandable title="Balance object properties">
<ResponseField name="balance_id" type="string" required>
Unique identifier for the balance.
</ResponseField>

<ResponseField name="ledger_id" type="string" required>
ID of the ledger this balance belongs to.
</ResponseField>

<ResponseField name="currency" type="string" required>
Currency code for the balance.
</ResponseField>

<ResponseField name="balance" type="number" required>
Current balance amount.
</ResponseField>

<ResponseField name="created_at" type="timestamp" required>
ISO 8601 formatted timestamp of when the balance was created.
</ResponseField>
</Expandable>

---

<Info>
For filters and supported fields, see the [Data API docs](/cloud/proxy/data-api).
</Info>

---

<NeedHelp />
