---
title: "Get identity"
api: "GET /data/identities/{identity_id}"
description: "Retrieve a single identity by ID from your Blnk Core instance."
---

import NeedHelp from "/cloud/snippets/need-help.mdx";

Retrieve details for a specific identity by its ID.

<Note>
For in-depth information about Blnk's ledger, balance, transaction, and identity APIs, see the [Blnk Core docs](https://docs.blnkfinance.com).
</Note>

---

### Request structure

<RequestExample>
```bash cURL
curl -X GET "https://api.cloud.blnkfinance.com/data/identities/idt_3b63c8da-af29-4cc3-ad38-df17d87456e6?instance_id=YOUR_INSTANCE_ID" \
  -H "Authorization: Bearer blnk_at_YOUR_ACCESS_TOKEN"
```
</RequestExample>

<ParamField path="identity_id" type="string" required>
Unique identifier of the identity to retrieve.
</ParamField>

<ParamField header="Authorization" type="string" required>
Bearer token for authentication. Format: `Bearer <access_token>`. Your OAuth key must include `data:read` scope (or `*`).
</ParamField>

---

### Query parameters

<ParamField query="instance_id" type="string" required>
The Blnk Core instance ID to read from. Find this in Blnk Cloud under Instances.
</ParamField>

---

### Response structure

<ResponseExample>
```json Success
{
  "identity_id": "idt_3b63c8da-af29-4cc3-ad38-df17d87456e6",
  "first_name": "John",
  "last_name": "Doe",
  "other_names": "",
  "email_address": "john.doe@example.com",
  "phone_number": "+1234567890",
  "nationality": "US",
  "gender": "male",
  "dob": "1990-01-01",
  "street": "123 Main St",
  "city": "New York",
  "state": "NY",
  "post_code": "10001",
  "country": "US",
  "organization_name": "",
  "category": "individual",
  "identity_type": "customer",
  "created_at": "2024-11-26T08:36:36.238244338Z",
  "meta_data": {
    "myApp": {
      "segment": "premium",
      "kyc_status": "verified"
    }
  }
}
```
</ResponseExample>

<ResponseField name="identity_id" type="string" required>
Unique identifier for the identity.
</ResponseField>

<ResponseField name="first_name" type="string">
First name of the identity.
</ResponseField>

<ResponseField name="last_name" type="string">
Last name of the identity.
</ResponseField>

<ResponseField name="other_names" type="string">
Other names or aliases for the identity.
</ResponseField>

<ResponseField name="email_address" type="string">
Email address of the identity.
</ResponseField>

<ResponseField name="phone_number" type="string">
Phone number of the identity.
</ResponseField>

<ResponseField name="nationality" type="string">
Nationality code of the identity.
</ResponseField>

<ResponseField name="gender" type="string">
Gender of the identity.
</ResponseField>

<ResponseField name="dob" type="string">
Date of birth of the identity.
</ResponseField>

<ResponseField name="street" type="string">
Street address of the identity.
</ResponseField>

<ResponseField name="city" type="string">
City of the identity.
</ResponseField>

<ResponseField name="state" type="string">
State or province of the identity.
</ResponseField>

<ResponseField name="post_code" type="string">
Postal code of the identity.
</ResponseField>

<ResponseField name="country" type="string">
Country code of the identity.
</ResponseField>

<ResponseField name="organization_name" type="string">
Organization name, if the identity is an organization.
</ResponseField>

<ResponseField name="category" type="string">
Category of the identity (e.g., `individual`, `organization`).
</ResponseField>

<ResponseField name="identity_type" type="string">
Type of the identity (e.g., `customer`, `vendor`).
</ResponseField>

<ResponseField name="created_at" type="timestamp" required>
ISO 8601 formatted timestamp of when the identity was created.
</ResponseField>

<ResponseField name="meta_data" type="object">
Metadata object attached to the identity, if any.
</ResponseField>

---

<Info>
For filters and supported fields, see the [Data API docs](/cloud/proxy/data-api).
</Info>

---

<NeedHelp />
