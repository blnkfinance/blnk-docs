---
title: "List deployments"
api: "GET /deployments"
description: "List managed Core instances in your Blnk Cloud workspace."
---

import NeedHelp from "/cloud/snippets/need-help.mdx";
import Authorization from "/cloud/snippets/authorization.mdx";
import { RelatedTopics } from "/snippets/related-topics.jsx";

Use this endpoint to list every deployment in the workspace, including `ACTIVE`, `PENDING`, `FAILED`, `DESTROYING`, and `DESTROYED` rows. See [Managed instances](/cloud/instances/deploy) to learn more.

<Note>
This endpoint lists managed instances only. For a connected (self-hosted) instance, [connect it from the dashboard](/cloud/instances/create) first. Self-hosted instances do not appear here.
</Note>

---

<Authorization />

<RequestExample>
```bash cURL
curl -X GET 'https://api.cloud.blnkfinance.com/deployments' \
  -H 'Authorization: Bearer CLOUD_API_KEY'
```
</RequestExample>

### Response

<ResponseExample>
```json 200
[
  {
    "deployment_id": "deploy_073f7ffe-9dfd-42ce-aa50-d1dca1788adc",
    "instance_id": "instance_073f7ffe-9dfd-42ce-aa50-d1dca1788adc",
    "name": "my-sandbox",
    "ingress_mode": "shared",
    "dedicated": false,
    "tier": "B1.Micro",
    "type": "sandbox",
    "region": "nyc3",
    "status": "ACTIVE",
    "domain": "my-sandbox-sharespaces.deployment.blnkfinance.com",
    "url": "https://my-sandbox-sharespaces.deployment.blnkfinance.com",
    "watch_enabled": false,
    "usage_billing_enabled": true,
    "usage_billing_enabled_at": "2026-09-09T07:20:08.028229Z",
    "record_creates_blocked": false,
    "created_at": "2026-09-11T09:19:05.954015Z",
    "updated_at": "2026-09-11T09:20:39.861066Z"
  }
]
```
</ResponseExample>

The response is a JSON array of deployment objects.

<ResponseField name="deployment_id" type="string">
Unique id for this managed deployment (`deploy_...`). Use it with Deployments APIs. Do not pass it as `instance_id` when routing Cloud APIs.
</ResponseField>

<ResponseField name="instance_id" type="string">
Unique id for the connected Core (`instance_...`). Pass it on Proxy, Filters, Data Lake, and other Cloud APIs so Cloud routes the request to this Core. Empty while the deployment is still being provisioned.
</ResponseField>

<ResponseField name="name" type="string">
Display name of the instance in your workspace.
</ResponseField>

<ResponseField name="type" type="string">
Deployment type: `sandbox` or `production`. Cloud assigns capacity from this value.
</ResponseField>

<ResponseField name="tier" type="string">
Capacity Cloud assigned for this deployment, such as `B1.Micro` or `B1.Standard`. You do not choose this when you create the deployment.
</ResponseField>

<ResponseField name="status" type="string">
Current state of the deployment: `PENDING`, `ACTIVE`, `DESTROYING`, `DESTROYED`, or `FAILED`. `PENDING` means Cloud is still provisioning. Sandbox typically takes 7 - 10 minutes. Production can take 10 - 30 minutes.
</ResponseField>

<ResponseField name="region" type="string">
Datacenter code for the instance. Cloud assigns this for sandbox. You choose it for production.
</ResponseField>

<ResponseField name="dedicated" type="boolean">
`true` when the instance runs on dedicated Production infrastructure. Sandbox is always `false`.
</ResponseField>

<ResponseField name="ingress_mode" type="string">
How Cloud routes traffic to the instance. Live values include `shared` and `legacy`.
</ResponseField>

<ResponseField name="domain" type="string">
Hostname of the Core once the instance is reachable.
</ResponseField>

<ResponseField name="url" type="string">
HTTPS Core URL once the instance is reachable. Use this to call Core directly, with the Core secret from [Get instance details](/cloud/reference/get-instance).
</ResponseField>

<ResponseField name="watch_enabled" type="boolean">
Whether Watch is enabled.
</ResponseField>

<ResponseField name="usage_billing_enabled" type="boolean">
Whether usage-based billing is on for the organization.
</ResponseField>

<ResponseField name="usage_billing_enabled_at" type="timestamp">
When usage-based billing was enabled for the organization.
</ResponseField>

<ResponseField name="record_creates_blocked" type="boolean">
`true` when this instance has used its included records and organization usage-based billing is off, so new records cannot be created. `false` while the instance is within its allowance or usage-based billing is on. See [Usage-based billing](/cloud/organization/billing#usage-based-billing).
</ResponseField>

<ResponseField name="created_at" type="timestamp">
When the deployment was created.
</ResponseField>

<ResponseField name="updated_at" type="timestamp">
When the deployment was last updated.
</ResponseField>

---

<NeedHelp />

<RelatedTopics
  items={[
    { title: "Managed instances", href: "/cloud/instances/deploy" },
    { title: "Create deployment", href: "/cloud/reference/create-deployment" },
    { title: "Get deployment details", href: "/cloud/reference/get-deployment" },
    { title: "Get instance details", href: "/cloud/reference/get-instance" },
  ]}
/>
