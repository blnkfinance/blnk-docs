---
title: "Get ledger"
api: "GET /data/ledgers/{ledger_id}"
description: "Retrieve a single ledger by ID from your Blnk Core instance."
---

import NeedHelp from "/cloud/snippets/need-help.mdx";

Retrieve details for a specific ledger by its ID.

<Note>
For in-depth information about Blnk's ledger, balance, transaction, and identity APIs, see the [Blnk Core docs](https://docs.blnkfinance.com).
</Note>

---

### Request structure

<RequestExample>
```bash cURL
curl -X GET "https://api.cloud.blnkfinance.com/data/ledgers/ldg_073f7ffe-9dfd-42ce-aa50-d1dca1788adc?instance_id=YOUR_INSTANCE_ID" \
  -H "Authorization: Bearer blnk_at_YOUR_ACCESS_TOKEN"
```
</RequestExample>

<ParamField path="ledger_id" type="string" required>
Unique identifier of the ledger to retrieve.
</ParamField>

<ParamField header="Authorization" type="string" required>
Bearer token for authentication. Format: `Bearer <access_token>`. Your OAuth key must include `data:read` scope (or `*`).
</ParamField>

---

### Query parameters

<ParamField query="instance_id" type="string" required>
The Blnk Core instance ID to read from. Find this in Blnk Cloud under Instances.
</ParamField>

---

### Response structure

<ResponseExample>
```json Success
{
  "ledger_id": "ldg_073f7ffe-9dfd-42ce-aa50-d1dca1788adc",
  "name": "MyApp Integration Ledger",
  "created_at": "2024-02-20T05:28:03.558281542Z",
  "meta_data": {
    "description": "Ledger for integration test"
  }
}
```
</ResponseExample>

<ResponseField name="ledger_id" type="string" required>
Unique identifier for the ledger.
</ResponseField>

<ResponseField name="name" type="string" required>
Name of the ledger.
</ResponseField>

<ResponseField name="created_at" type="timestamp" required>
ISO 8601 formatted timestamp of when the ledger was created.
</ResponseField>

<ResponseField name="meta_data" type="object">
Metadata object attached to the ledger, if any.
</ResponseField>

---

<Info>
For filter queries and supported operators, see the [Data API docs](/cloud/proxy/data-api).
</Info>

---

<NeedHelp />
