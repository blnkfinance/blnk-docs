---
title: "Using API keys"
sidebarTitle: "Using API keys"
icon: "lock-keyhole"
description: "Learn how to create and manage API keys for access to Blnk Cloud APIs."
og:title: "Using API keys | Blnk Cloud Guide"
og:description: "Create and manage API keys to authenticate programmatically with Blnk Cloud APIs"
---

import NeedHelp from "/cloud/snippets/need-help.mdx";
import { RelatedTopics } from "/snippets/related-topics.jsx";

You can authenticate with Blnk Cloud APIs using API keys or [OAuth](/cloud/reference/oauth). To create and manage your API keys,  [Settings > API Keys](https://cloud.blnkfinance.com/cloud/settings/api-keys).

<Note>
API keys for Cloud APIs are different from API keys for Blnk Core. If calling the Core API directly, you'll need to set up your API keys directly in Core. 
</Note>

---

## How Cloud API keys work

When you make a Cloud API call, send the key in the `Authorization: Bearer CLOUD_API_KEY` header.

The base URL is `https://api.cloud.blnkfinance.com`. Self-hosted Cloud under the Production License use `ENTERPRISE_PUBLIC_URL`.

Each API key has:
- **Name:** A user-friendly identifier to help you organize and identify keys.
- **Key prefix:** A partial identifier visible in the API keys list (e.g., `blnk_fdb796a5...`).
- **Scopes:** Permissions that define what the key can access, for example `*`, `mcp:read`, `mcp:write`, etc.
- **Expiration:** Optional expiration date, or **Never** for keys you don't want to expire.

---

## Create an API key

<Steps>
  <Step title="Go to settings">
    Go to [Settings > API Keys](https://cloud.blnkfinance.com/cloud/settings/api-keys). Click **Create API key** in the top-right corner of the page.

    <Frame>
      <img
        src="/cloud/img/auth/api-keys-create-button.png"
        alt="API keys page with the Create API key button highlighted"
        className="rounded-lg"
      />
    </Frame>
  </Step>
  <Step title="Configure your API key">
    In the **Create API key** panel, fill in the required details:

    - **Name:** A descriptive name. Best to use the name of the user or integration that will be using the key, e.g. alerts integration, Emily Blunt, etc.
    - **Type:** Select **API Key**.
    - **Expiration:** Choose when the key should expire
    - **Scopes:** Select the permissions this key needs

    <Frame>
      <img
        src="/cloud/img/auth/api-keys-create-panel.png"
        alt="Create API key panel with name, type, expiration, and scopes"
        className="rounded-lg"
      />
    </Frame>
  </Step>
  <Step title="Save and copy your API key">
    Click **Create API key**. The full key is shown only once on the **Save your API key** screen. Copy it and store it securely before clicking **Done**.

    <Frame>
      <img
        src="/cloud/img/auth/api-keys-save.png"
        alt="Save your API key panel showing the full key and expiration details"
        className="rounded-lg"
      />
    </Frame>

    <Warning>
      You cannot retrieve the full API key after creation. If you lose the key, you must void the key and create a new one.
    </Warning>
  </Step>
</Steps>

---

## Update expiration

You can change when an API key expires without creating a new key.

<Steps>
  <Step title="Open API key details">
    Click an API key name in the API Keys table to open its details panel. In **Quick actions**, click **Update expiration**.

    <Frame>
      <img
        src="/cloud/img/auth/api-key-expiration.png"
        alt="API key details panel with Update expiration highlighted in Quick actions"
        className="rounded-lg"
      />
    </Frame>
  </Step>
  <Step title="Choose a new expiration date">
    Select a new expiration date, then confirm your change. The key keeps working with the same secret. Only its expiration date changes.

    <Frame>
      <img
        src="/cloud/img/auth/api-key-update-expiration-modal.png"
        alt="Update API key expiration modal with expiration options"
        className="rounded-lg"
      />
    </Frame>
  </Step>
</Steps>

---

## Revoke an API key

If you need to disable an API key without deleting it permanently, you can revoke it. Revoked keys cannot be used for authentication but remain visible in your API keys list for reference.

<Steps>
  <Step title="Open API key details">
    Click the API key name in the API Keys table to view its details.
  </Step>

  <Step title="Revoke the key">
    In the API key details panel, click `Revoke`.

    <Warning>
      Revoking a key will immediately disable it. Any applications or scripts using this key will stop working until you create and configure a new one.
    </Warning>
  </Step>

  <Step title="Confirm revocation">
    Confirm that you want to revoke the key. The key's status will change to **Revoked** in the API Keys table.
  </Step>
</Steps>

{/* ---

## Scopes

Pick the smallest set of scopes the key needs. `read` covers GET. `write` covers create, update, and delete.

| Scope | Path |
| :-- | :-- |
| `deployments:read` / `deployments:write` | `/deployments` |
| `data:read` / `data:write` | `/data`, `/proxy`, filter POSTs |
| `proxy:read` / `proxy:write` | `/proxy` |
| `alerts:read` / `alerts:write` | `/alerts` |
| `billing:read` / `billing:write` | `/billing` |
| `audit:read` | `/audit` |
| `mcp:read` / `mcp:write` | `/mcp` |
| `apps:read` / `apps:write` | `/apps` |
| `*` | All Cloud scopes above |

`POST /data/lake/query` accepts `data:read` or `data:write`. App install and portal still use a dashboard session.

To run the Cloud APIs in this reference from one key, use:

```text
deployments:read, deployments:write,
data:read, data:write,
proxy:read, proxy:write,
alerts:read, alerts:write,
billing:read, billing:write,
audit:read
```

<Warning>
Avoid `*` unless you need every Cloud scope. Create a scoped key for each integration.
</Warning> */}

---

<NeedHelp />

<RelatedTopics
  items={[
    { title: "OAuth", href: "/cloud/reference/oauth" },
    { title: "Cloud API overview", href: "/cloud/reference/overview" },
    { title: "Core API keys", href: "/api-keys/overview" },
  ]}
/>
