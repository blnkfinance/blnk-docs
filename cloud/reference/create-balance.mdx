---
title: "Create balance"
api: "POST /cloud/proxy/balances"
description: "Create a new balance in your Blnk Core instance via the Cloud Proxy."
---

import NeedHelp from "/cloud/snippets/need-help.mdx";

Create a new balance in a ledger. Balances track amounts in a specific currency and can be used as sources or destinations for transactions.

<Note>
For in-depth information about Blnk's ledger, balance, transaction, and identity APIs, see the [Blnk Core docs](https://docs.blnkfinance.com).
</Note>

---

### Request structure

<RequestExample>
```bash cURL
curl -X POST "https://api.cloud.blnkfinance.com/proxy/balances?instance_id=YOUR_INSTANCE_ID" \
  -H "Authorization: Bearer blnk_at_YOUR_ACCESS_TOKEN" \
  -H "Content-Type: application/json" \
  -d '{
    "ledger_id": "ldg_073f7ffe-9dfd-42ce-aa50-d1dca1788adc",
    "currency": "USD"
  }'
```
</RequestExample>

<ParamField header="Authorization" type="string" required>
Bearer token for authentication. Format: `Bearer <access_token>`. Your OAuth key must include `proxy:write` scope (or `*`).
</ParamField>

<ParamField body="ledger_id" type="string" required>
The ID of the ledger to create the balance in.
</ParamField>

<ParamField body="identity_id" type="string">
The ID of the ledger to create the balance in.
</ParamField>

<ParamField body="currency" type="string" required>
Currency code for the balance (e.g., `USD`, `EUR`, `GBP`).
</ParamField>

<ParamField body="meta_data" type="object">
Optional metadata object for custom key-value data. Use a top-level key (e.g., your app name) to avoid conflicts.
</ParamField>

---

### Query parameters

<ParamField query="instance_id" type="string" required>
The Blnk Core instance ID to target. Find this in Blnk Cloud under Instances.
</ParamField>

---

### Response structure

<ResponseExample>
```json Success
{
  "balance": 0,
  "inflight_balance": 0,
  "credit_balance": 0,
  "inflight_credit_balance": 0,
  "debit_balance": 0,
  "inflight_debit_balance": 0,
  "ledger_id": "ldg_073f7ffe-9dfd-42ce-aa50-d1dca1788adc",
  "identity_id": "",
  "balance_id": "bln_5ce86029-3c2e-4e2a-aae2-7fb931ca4c4f",
  "currency": "USD",
  "created_at": "2024-11-26T08:36:36.238244338Z",
  "meta_data": null
}
```
</ResponseExample>

<ResponseField name="balance_id" type="string" required>
Unique identifier for the created balance.
</ResponseField>

<ResponseField name="ledger_id" type="string" required>
The ID of the ledger this balance belongs to.
</ResponseField>

<ResponseField name="currency" type="string" required>
Currency code for the balance.
</ResponseField>

<ResponseField name="balance" type="number" required>
Current balance amount.
</ResponseField>

<ResponseField name="inflight_balance" type="number" required>
Balance amount for in-flight transactions.
</ResponseField>

<ResponseField name="credit_balance" type="number" required>
Total credit balance.
</ResponseField>

<ResponseField name="debit_balance" type="number" required>
Total debit balance.
</ResponseField>

<ResponseField name="inflight_credit_balance" type="number" required>
In-flight credit balance.
</ResponseField>

<ResponseField name="inflight_debit_balance" type="number" required>
In-flight debit balance.
</ResponseField>

<ResponseField name="identity_id" type="string">
ID of the identity associated with this balance, if any.
</ResponseField>

<ResponseField name="created_at" type="timestamp" required>
ISO 8601 formatted timestamp of when the balance was created.
</ResponseField>

<ResponseField name="meta_data" type="object">
The metadata object attached to the balance, if provided.
</ResponseField>

---

<NeedHelp />
