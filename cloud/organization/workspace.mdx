---
title: "Workspace"
sidebarTitle: "Workspace"
icon: "building-2"
description: "Learn how to set up your workspace."
og:title: "Workspace | Blnk Cloud Guide"
og:description: "Set up a workspace in Blnk Cloud, organize projects, manage resources, and segment your workflows."
---

import NeedHelp from "/cloud/snippets/need-help.mdx";

Your workspace is the control center of all instances and data in Blnk Cloud. Each organization has one workspace. If you belong to more than one organization, you can switch between them without signing out.

You can update your current workspace in [Settings > Workspace](https://cloud.blnkfinance.com/cloud/settings/workspace).

<img src="/cloud/img/organization/workspace-domains.png" alt="Workspace settings page showing organization details and the Domains section" className="rounded-lg" />

You can update:

- Organization name
- Team size
- Country

---

## Workspace domains

Verify domains your organization owns so teammates with a matching email address can request to join your workspace, without needing a manual invite.

<Steps>
  <Step title="Open the Domains section">
    Go to [Settings > Workspace](https://cloud.blnkfinance.com/cloud/settings/workspace) and scroll to the `Domains` section.
  </Step>
  <Step title="Start adding a domain">
    Click `Add domain` at the top right of the `Domains` section.

    <Frame>
      <img
        src="/cloud/img/organization/workspace-add-domain-button.png"
        alt="Domains section with the Add domain button highlighted"
        className="rounded-lg"
      />
    </Frame>
  </Step>
  <Step title="Enter domain details">
    In the `Add domain` panel that opens, enter the `Domain` you own (for example, `company.com`) and a `Verification email` on that domain. Click `Send verification code`.

    <Frame>
      <img
        src="/cloud/img/organization/workspace-add-domain-panel.png"
        alt="Add domain side panel with Domain and Verification email fields"
        className="rounded-lg"
      />
    </Frame>
  </Step>
  <Step title="Confirm ownership">
    Enter the verification code sent to your verification email. Once confirmed, the domain appears in the `Domains` tab.
  </Step>
  <Step title="Review join requests">
    Switch to the `Join requests` tab to approve or decline teammates who signed up with an email on a verified domain.

    <Frame>
      <img
        src="/cloud/img/organization/workspace-join-requests.png"
        alt="Join requests tab showing teammates requesting to join the workspace"
        className="rounded-lg"
      />
    </Frame>
  </Step>
</Steps>

<Tip>
Domain-based join requests are separate from [team invitations](/cloud/organization/team). Admins can still invite anyone by email from **Settings > Team members**.
</Tip>

---

## Switch workspace

If you belong to more than one organization in Blnk Cloud, you can move between workspaces without signing out.

<Steps>
  <Step title="Open your workspace menu">
    At the top of the left sidebar, click your `workspace name or logo`.

    <Frame>
      <img
        src="/cloud/img/organization/switch-workspace-sidebar-menu.png"
        alt="Sidebar workspace control with the workspace menu open"
        className="rounded-lg"
      />
    </Frame>

    Select `Switch workspace`.
  </Step>
  <Step title="Choose a workspace">
    Pick the workspace you want to open. Your current workspace is highlighted in the list.

    <Frame>
      <img
        src="/cloud/img/organization/switch-workspace-list.png"
        alt="Switch workspace list showing available workspaces with the current one highlighted"
        className="rounded-lg"
      />
    </Frame>

    You may see a brief loading state while the switch completes.
  </Step>
</Steps>

When the switch finishes, your dashboard updates to show the workspace you selected. If that workspace still needs setup, Blnk Cloud will walk you through it before you continue.

<Tip>
If something looks missing after a switch, check that you are in the right workspace and instance. Data does not carry over between workspaces.
</Tip>

---

## Add workspace

Need a separate workspace for another organization? You can create one from the same `Switch workspace` menu.

<Steps>
  <Step title="Open your workspace menu">
    At the top of the left sidebar, click your `workspace name or logo`, then select `Switch workspace`.

    <Frame>
      <img
        src="/cloud/img/organization/switch-workspace-sidebar-menu.png"
        alt="Sidebar workspace menu with Switch workspace option"
        className="rounded-lg"
      />
    </Frame>
  </Step>
  <Step title="Create the new workspace">
    Click `Add workspace`, then enter your organization name, team size, and country.

    <Frame>
      <img
        src="/cloud/img/organization/add-workspace-form.png"
        alt="Add workspace form with organization name, size, and country fields"
        className="rounded-lg"
      />
    </Frame>

    Click `Create workspace` to finish. Blnk Cloud creates the workspace and opens it for you automatically.
  </Step>
</Steps>

---

<NeedHelp />

