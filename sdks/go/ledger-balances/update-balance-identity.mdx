---
mode: "wide"
title: "Update balance identity"
sidebarTitle: "Update balance identity"
api: "PUT /balances/{balance_id}/identity"
description: "Update the identity associated with a balance with the Go SDK."
---

import NeedHelp from "/snippets/need-help.mdx";
import { RelatedTopics } from "/snippets/related-topics.jsx";

Use `client.LedgerBalance.UpdateIdentity` to link a balance to an [identity](/identities/link-balances).

<Steps>
<Step title="Call the method">

```go client.LedgerBalance.UpdateIdentity wrap
result, resp, err := client.LedgerBalance.UpdateIdentity(
	"bln_5ce86029-3c2e-4e2a",
	blnkgo.UpdateBalanceIdentityRequest{
		IdentityID: "idt_3b63c8da-af29-4cc3",
	},
)
```

| Field | Type | Description |
| :-- | :-- | :-- |
| `balanceID` | `string` | Balance ID to update. |
| `IdentityID` | `string` | Identity ID to link to the balance. |

</Step>

<Step title="Response">

```json 200 OK wrap
{
  "message": "Balance identity updated successfully"
}
```

#### Go response (`*blnkgo.UpdateBalanceIdentityResponse`)

| Field | Type | Description |
| :-- | :-- | :-- |
| `Message` | `string` | Confirmation from Core. |

</Step>
</Steps>

---

<NeedHelp />

<RelatedTopics
  items={[
    { title: "Link balances", href: "/identities/link-balances" },
    { title: "Identities overview", href: "/identities/introduction" },
    { title: "Update balance identity", href: "/reference/update-balance-identity" },
  ]}
/>
