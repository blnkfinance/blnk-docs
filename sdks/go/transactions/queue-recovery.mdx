---
mode: "wide"
title: "Queue recovery"
sidebarTitle: "Recover queued transactions"
api: "POST /transactions/recover"
description: "Recover stuck queued transactions with the Go SDK."
---

import NeedHelp from "/snippets/need-help.mdx";
import { RelatedTopics } from "/snippets/related-topics.jsx";

Use `client.Transaction.RecoverQueue` to retry queued transactions.

<Steps>
<Step title="Call the method">

```go client.Transaction.RecoverQueue wrap
result, resp, err := client.Transaction.RecoverQueue(blnkgo.RecoverQueueRequest{
	Threshold: "5m",
})
```

| Field | Type | Description |
| :-- | :-- | :-- |
| `Threshold` | `string` | Minimum age of transactions to recover (for example `5m`, `1h`). Default `2m`. Minimum 2 minutes. |

</Step>

<Step title="Response">

```json 200 OK wrap
{
  "recovered": 3,
  "threshold": "5m0s"
}
```

#### Go response (`*blnkgo.RecoverQueueResponse`)

| Field | Type | Description |
| :-- | :-- | :-- |
| `Recovered` | `int` | Number of transactions re-enqueued for processing. |
| `Threshold` | `string` | Threshold duration that was used. |

</Step>
</Steps>

---

<NeedHelp />

<RelatedTopics
  items={[
    { title: "Queue recovery", href: "/advanced/queue-recovery" },
    { title: "Concurrency", href: "/guides/concurrency" },
    { title: "Handling hot balances", href: "/guides/hot-balances" },
    { title: "Recover queued transactions", href: "/reference/queue-recovery" },
  ]}
/>
