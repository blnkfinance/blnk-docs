---
mode: "wide"
title: "Get health status"
sidebarTitle: "Get health status"
api: "GET /health"
description: "Check whether Blnk Core is running and reachable with the Go SDK."
---

import NeedHelp from "/snippets/need-help.mdx";

Use `client.Health.Check` to check whether Core is reachable.

<Steps>
<Step title="Call the method">

```go client.Health.Check wrap
health, resp, err := client.Health.Check()
```

This method takes no arguments.

</Step>

<Step title="Confirm Core is healthy">

Check that `health.Status` is `UP` before running other SDK calls in health checks or startup probes.

</Step>

<Step title="Response">

```json 200 OK wrap
{
  "status": "UP"
}
```

#### Go response (`*blnkgo.HealthResponse`)

| Field | Type | Description |
| :-- | :-- | :-- |
| `Status` | `string` | `UP` when Core is healthy and reachable. |
| `Reason` | `string` | Optional detail when Core reports an unhealthy state. |

</Step>
</Steps>

---

## Related docs

<CardGroup cols={2}>
<Card title="Set up the SDK" icon="code" href="/sdks/go/start/using-the-sdk">
Initialize the client and configure the base URL.
</Card>

<Card title="Health check" icon="book-open" href="/reference/get-health">
HTTP request and response schema.
</Card>
</CardGroup>

---

<NeedHelp />
