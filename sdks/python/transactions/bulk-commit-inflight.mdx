---
mode: "wide"
title: "Bulk commit inflight"
sidebarTitle: "Bulk commit inflight"
api: "POST /transactions/inflight/bulk/commit"
description: "Commit multiple independently-created inflight transactions with the Python SDK."
---

import NeedHelp from "/snippets/need-help.mdx";
import { RelatedTopics } from "/snippets/related-topics.jsx";

Use `blnk.transactions.bulk_commit_inflight` to commit multiple inflight transactions.

<Steps>
<Step title="Call the method">

<Tabs>
<Tab title="Queued commit">

```python blnk.transactions.bulk_commit_inflight wrap
response = blnk.transactions.bulk_commit_inflight({
  "transactions": [
    {
      "transaction_id": "txn_c4e70eb8-e4d6-4e04-a2e2-92a43b96",
    },
    {
      "transaction_id": "txn_6164573b-6cc8-45a4-ad2e-7b4ba6a6",
      "amount": 40,
    },
    {
      "transaction_id": "txn_0b59f6e6-6c4a-4efa-915c-526f77ef",
      "precise_amount": 125034,
    },
  ],
})
```

</Tab>

<Tab title="Synchronous commit">

Pass `skip_queue: True` to process each commit synchronously instead of queuing.

```python blnk.transactions.bulk_commit_inflight wrap
response = blnk.transactions.bulk_commit_inflight({
  "skip_queue": True,
  "transactions": [
    {
      "transaction_id": "txn_c4e70eb8-e4d6-4e04-a2e2-92a43b96",
    },
    {
      "transaction_id": "txn_6164573b-6cc8-45a4-ad2e-7b4ba6a6",
      "amount": 40,
    },
  ],
})
```

</Tab>
</Tabs>

| Field | Type | Description |
| :-- | :-- | :-- |
| `transactions` | `list[dict]` | Up to 100 inflight transactions to commit. |
| `transactions.`<br />`[].transaction_id` | `str` | Inflight transaction ID to commit. |
| `transactions.`<br />`[].amount` | `number` | Partial commit amount. Omit to commit the full remaining inflight amount. |
| `transactions.`<br />`[].precise_amount` | `number \| str` | Precision-applied partial commit amount. Takes precedence over amount. |
| `skip_queue` | `bool` | When `True`, process synchronously instead of queuing. |

</Step>

<Step title="Review per-item results">

Check `response.data["results"]` for each transaction's outcome. Failed items include a code and message without stopping the rest of the batch.

</Step>

<Step title="Response">

```json 200 OK wrap expandable
{
  "succeeded": 2,
  "failed": 1,
  "results": [
    {
      "transaction_id": "txn_c4e70eb8-e4d6-4e04-a2e2-92a43b96",
      "status": "succeeded"
    },
    {
      "transaction_id": "txn_6164573b-6cc8-45a4-ad2e-7b4ba6a6",
      "status": "succeeded"
    },
    {
      "transaction_id": "txn_0b59f6e6-6c4a-4efa-915c-526f77ef",
      "status": "failed",
      "code": "INVALID_AMOUNT",
      "message": "cannot commit more than inflight amount"
    }
  ]
}
```

| Field | Type | Description |
| :-- | :-- | :-- |
| `succeeded` | `number` | Number committed successfully. |
| `failed` | `number` | Number that failed. |
| `results` | `list[dict]` | Per-transaction outcome in request order. |
| `results[].transaction_id` | `str` | Transaction ID from the request. |
| `results[].status` | `str` | `succeeded`, `failed`, or `queued` (when `skip_queue` is `False`). |
| `results[].code` | `str` | Failure code (for example `NOT_FOUND`, `ALREADY_VOIDED`, `INVALID_AMOUNT`). |
| `results[].message` | `str` | Human-readable failure message. |

</Step>
</Steps>

---

<NeedHelp />

<RelatedTopics
  items={[
    { title: "Bulk commit & void", href: "/transactions/inflight/bulk-update-inflight" },
    { title: "Commit & void inflight", href: "/transactions/inflight/updating-inflight" },
    { title: "Handling hot balances", href: "/guides/hot-balances" },
    { title: "Bulk Commit Inflight", href: "/reference/bulk-commit-inflight" },
  ]}
/>
