---
mode: "wide"
title: "Get health status"
sidebarTitle: "Get health status"
api: "GET /health"
description: "Check whether Blnk Core is running and reachable with the Python SDK."
---

import NeedHelp from "/snippets/need-help.mdx";

Use `blnk.system.health` to check whether Core is reachable.

<Steps>
<Step title="Call the method">

```python blnk.system.health wrap
response = blnk.system.health()
```

This method takes no arguments.

</Step>

<Step title="Confirm Core is healthy">

Check that `response.data["status"]` is `UP` before running other SDK calls in health checks or startup probes.

</Step>

<Step title="Response">

```json 200 OK wrap
{
  "status": "UP"
}
```

| Field | Type | Description |
| :-- | :-- | :-- |
| `status` | `str` | `UP` when Core is healthy and reachable. |

</Step>
</Steps>

---

## Related docs

<CardGroup cols={2}>
<Card title="Set up the SDK" icon="code" href="/sdks/python/start/using-the-sdk">
Initialize the client and configure the base URL.
</Card>

<Card title="Health check" icon="book-open" href="/reference/get-health">
HTTP request and response schema.
</Card>
</CardGroup>

---

<NeedHelp />
