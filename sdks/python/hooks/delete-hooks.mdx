---
mode: "wide"
title: "Delete hook"
sidebarTitle: "Delete hook"
api: "DELETE /hooks/{id}"
description: "Delete a hook with the Python SDK."
---

import NeedHelp from "/snippets/need-help.mdx";
import { RelatedTopics } from "/snippets/related-topics.jsx";

Use `blnk.hooks.delete` to permanently remove a [webhook](/hooks/overview) by its ID.

<Steps>
<Step title="Call the method">

```python blnk.hooks.delete wrap
response = blnk.hooks.delete("hk_test_123")
```

| Field | Type | Description |
| :-- | :-- | :-- |
| `id` | `str` | ID of the hook to delete. |

</Step>

<Step title="Confirm the outcome">

Check the response message to confirm the hook was removed. Blnk will no longer call this endpoint on transactions.

</Step>

<Step title="Response">

```json 200 OK wrap
{
  "message": "hook deleted successfully"
}
```

| Field | Type | Description |
| :-- | :-- | :-- |
| `message` | `str` | Confirmation that the hook was deleted. |

</Step>
</Steps>

---

<NeedHelp />

<RelatedTopics
  items={[
    { title: "Transaction hooks", href: "/webhooks/transaction-hooks" },
    { title: "Webhooks overview", href: "/webhooks/overview" },
    { title: "Supported events", href: "/webhooks/events" },
    { title: "Delete Hooks", href: "/reference/delete-hooks" },
  ]}
/>
