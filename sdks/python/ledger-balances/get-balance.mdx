---
mode: "wide"
title: "View balance details"
sidebarTitle: "View balance details"
api: "GET /balances/{balance_id}"
description: "Get a balance by ID with the Python SDK."
---

import NeedHelp from "/snippets/need-help.mdx";
import { RelatedTopics } from "/snippets/related-topics.jsx";

Use `blnk.ledger_balances.get` to retrieve a balance by its balance ID.

<Steps>
<Step title="Call the method">

```python blnk.ledger_balances.get wrap
response = blnk.ledger_balances.get(
  "bln_5ce86029-3c2e-4e2a-aae2-7fb931ca4c4f",
)
```

| Field | Type | Description |
| :-- | :-- | :-- |
| `id` | `str` | Balance ID to retrieve. |
| `with_queued` | `bool` | Optional. When `True`, includes `queued_credit_balance` and `queued_debit_balance` in the response. |

Pass an options dict as the second argument to include queued balances:

```python blnk.ledger_balances.get with queued wrap
response = blnk.ledger_balances.get(
  "bln_5ce86029-3c2e-4e2a-aae2-7fb931ca4c4f",
  {"with_queued": True},
)
```

</Step>

<Step title="Use the balance details">

Read `response.data` for current amounts, currency, and ledger membership. Use `balance_id` when posting transactions or configuring monitors.

</Step>

<Step title="Response">

```json 200 OK wrap
{
  "balance": 0,
  "inflight_balance": 0,
  "credit_balance": 0,
  "inflight_credit_balance": 0,
  "debit_balance": 0,
  "inflight_debit_balance": 0,
  "ledger_id": "ldg_049495c6-356e-4ebc-a45e-60d1e1e16afb",
  "identity_id": "",
  "balance_id": "bln_5ce86029-3c2e-4e2a-aae2-7fb931ca4c4f",
  "currency": "USD",
  "created_at": "2024-11-26T08:36:36.238244338Z",
  "meta_data": null
}
```

| Field | Type | Description |
| :-- | :-- | :-- |
| `balance_id` | `str` | Unique ID for the balance. |
| `balance` | `number` | Current net amount. |
| `currency` | `str` | Currency or asset class. |
| `credit_balance` | `number` | Total amount moved into the balance. |
| `debit_balance` | `number` | Total amount moved out of the balance. |

</Step>
</Steps>

---

<NeedHelp />

<RelatedTopics
  items={[
    { title: "Balances overview", href: "/balances/introduction" },
    { title: "Internal balances", href: "/balances/internal-balances" },
    { title: "Handling hot balances", href: "/guides/hot-balances" },
    { title: "View balance details", href: "/reference/get-balance" },
  ]}
/>
