---
mode: "wide"
title: "Get tokenized fields"
sidebarTitle: "Get tokenized fields"
api: "GET /identities/{identity_id}/tokenized-fields"
description: "See which identity fields are tokenized with the Python SDK."
---

import NeedHelp from "/snippets/need-help.mdx";

Use `blnk.identity.get_tokenized_fields` to list tokenized identity fields.

<Steps>
<Step title="Call the method">

```python blnk.identity.get_tokenized_fields wrap
response = blnk.identity.get_tokenized_fields(
  "idt_3b63c8da-af29-4cc3-ad38-df17d87456e6",
)
```

| Field | Type | Description |
| :-- | :-- | :-- |
| `id` | `str` | Identity ID. |

</Step>

<Step title="Use the field list">

Read `response.data["tokenized_fields"]` to see which profile fields are protected. Use this before detokenizing specific fields.

</Step>

<Step title="Response">

```json 200 OK wrap
{
  "tokenized_fields": ["FirstName", "LastName", "EmailAddress", "PhoneNumber"]
}
```

| Field | Type | Description |
| :-- | :-- | :-- |
| `tokenized_fields` | `string[]` | Field names currently tokenized on the identity. |

</Step>
</Steps>

---

## Related docs

<CardGroup cols={2}>
<Card title="PII tokenization" icon="lock" href="/identities/pii-tokenization">
Setup and how tokenization works.
</Card>

<Card title="View tokenized fields" icon="book-open" href="/reference/get-tokenized-fields">
HTTP request and response schema.
</Card>
</CardGroup>

---

<NeedHelp />
