---
mode: "wide"
title: "Upload data"
sidebarTitle: "Upload data"
api: "POST /reconciliation/upload"
description: "Upload external data for reconciliation with the TypeScript SDK."
---

import NeedHelp from "/snippets/need-help.mdx";

Use `blnk.Reconciliation.upload` to upload external statement data.

<Steps>
<Step title="Call the method">

```typescript blnk.Reconciliation.upload wrap
const response = await blnk.Reconciliation.upload('statement.csv', 'stripe');
```

| Field | Type | Description |
| :-- | :-- | :-- |
| `fileInput` | `string` | Path to a CSV or JSON file, or a readable stream containing the external records. |
| `source` | `string` | Label describing where the data came from, for example `stripe`. |

</Step>

<Step title="Save the upload ID">

Use `response.data.upload_id` as the `upload_id` when you [start reconciliation](/sdks/typescript/reconciliation/start-reconciliation).

</Step>

<Step title="Response">

```json 201 Created wrap
{
  "upload_id": "upload_8c700d1b-09c0-4ef4-9ab1-a0decf3d0aa3",
  "record_count": 1000,
  "source": "Stripe"
}
```

| Field | Type | Description |
| :-- | :-- | :-- |
| `upload_id` | `string` | Unique ID for the upload. Store it to start a batch reconciliation run. |
| `record_count` | `number` | Number of records found in the file. |
| `source` | `string` | Label you passed for the data origin. |

</Step>
</Steps>

---

## Related docs

<CardGroup cols={2}>
<Card title="How reconciliation works" icon="scale" href="/reconciliations/overview">
Batch and instant reconciliation workflows.
</Card>

<Card title="Upload external records" icon="book-open" href="/reference/upload-data">
HTTP request and response schema.
</Card>
</CardGroup>

---

<NeedHelp />
