---
mode: "wide"
title: "Edit identity"
sidebarTitle: "Edit identity"
api: "PUT /identities/{identity_id}"
description: "Update an identity with the TypeScript SDK."
---

import NeedHelp from "/snippets/need-help.mdx";
import { RelatedTopics } from "/snippets/related-topics.jsx";

Use `blnk.Identity.update` to change the profile details on an [identity](/identities/introduction).

<Steps>
<Step title="Call the method">

```typescript blnk.Identity.update wrap
const response = await blnk.Identity.update(
  'idt_3b63c8da-af29-4cc3-ad38-df17d87456e6',
  {
    identity_type: 'individual',
    first_name: 'Jane',
    last_name: 'Smith',
  },
);
```

| Field | Type | Description |
| :-- | :-- | :-- |
| `id` | `string` | Identity ID to update. |
| `identity_type` | `string` | `individual` or `organization`. |
| Profile fields | `string` | Any field from [create](/sdks/typescript/identities/create-identity) you want to change. |

</Step>

<Step title="Verify the changes">

Read `response.data` to confirm the updated profile fields. The identity ID stays the same.

</Step>

<Step title="Response">

```json 200 OK wrap
{
  "identity_id": "idt_3b63c8da-af29-4cc3-ad38-df17d87456e6",
  "identity_type": "individual",
  "first_name": "Jane",
  "last_name": "Smith",
  "email_address": "alice.smith@example.com",
  "created_at": "2024-11-26T08:36:36.238244338Z"
}
```

| Field | Type | Description |
| :-- | :-- | :-- |
| `identity_id` | `string` | Unique ID for the identity. |
| `created_at` | `string` | Date and time the identity was created. |

The response returns the full identity with your changes applied.

</Step>
</Steps>

---

<NeedHelp />

<RelatedTopics
  items={[
    { title: "Identities overview", href: "/identities/introduction" },
    { title: "Link balances", href: "/identities/link-balances" },
    { title: "PII tokenization", href: "/identities/pii-tokenization" },
    { title: "Edit Identity", href: "/reference/edit-identity" },
  ]}
/>
