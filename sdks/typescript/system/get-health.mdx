---
mode: "wide"
title: "Get health status"
sidebarTitle: "Get health status"
api: "GET /health"
description: "Check whether Blnk Core is running and reachable with the TypeScript SDK."
---

import NeedHelp from "/snippets/need-help.mdx";
import { RelatedTopics } from "/snippets/related-topics.jsx";

Use `blnk.System.health` to check whether Core is reachable.

<Steps>
<Step title="Call the method">

```typescript blnk.System.health wrap
const response = await blnk.System.health();
```

This method takes no arguments.

</Step>

<Step title="Confirm Core is healthy">

Check that `response.data.status` is `UP` before running other SDK calls in health checks or startup probes.

</Step>

<Step title="Response">

```json 200 OK wrap
{
  "status": "UP"
}
```

| Field | Type | Description |
| :-- | :-- | :-- |
| `status` | `string` | `UP` when Core is healthy and reachable. |

</Step>
</Steps>

---

<NeedHelp />

<RelatedTopics
  items={[
    { title: "Monitoring", href: "/advanced/monitoring" },
    { title: "Get health", href: "/reference/get-health" },
    { title: "Deploy", href: "/home/deploy" },
  ]}
/>
