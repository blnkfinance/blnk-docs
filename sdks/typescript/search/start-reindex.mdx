---
mode: "wide"
title: "Start reindex"
sidebarTitle: "Start reindex"
api: "POST /search/reindex"
description: "Trigger a Typesense reindex from your existing database with the TypeScript SDK."
---

import NeedHelp from "/snippets/need-help.mdx";

Use `blnk.Search.startReindex` to rebuild the search index.

```typescript blnk.Search.startReindex wrap {1}
const response = await blnk.Search.startReindex({ batch_size: 1000 });
```

| Field | Description |
| :-- | :-- |
| `batch_size` | Records processed per batch. Omit the argument to use the Core default. Must be a positive integer when set. |

---

## Response shape

```json 202 Accepted wrap
{
  "message": "Reindex operation started",
  "progress": {
    "status": "in_progress",
    "phase": "indexing_transactions",
    "total_records": 2145709,
    "processed_records": 0,
    "started_at": "2026-02-12T12:13:10.448612096Z"
  }
}
```

| Field | Description |
| :-- | :-- |
| `message` | Confirmation that the reindex job started. |
| `progress` | Initial progress snapshot with `status`, `phase`, `total_records`, `processed_records`, and `started_at`. |

---

## Related docs

<CardGroup cols={2}>
<Card title="How reindexing works" icon="rotate" href="/search/typesense/reindex">
When and why to rebuild the index.
</Card>

<Card title="Start Typesense reindex" icon="book-open" href="/reference/start-reindex">
HTTP request and response schema.
</Card>
</CardGroup>

---

<NeedHelp />
