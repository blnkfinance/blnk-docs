---
mode: "wide"
title: "Get reindex status"
sidebarTitle: "Get reindex status"
api: "GET /search/reindex"
description: "Poll the progress of a Typesense reindex with the TypeScript SDK."
---

import NeedHelp from "/snippets/need-help.mdx";

Use `blnk.Search.getReindexStatus` to check search reindex progress.

```typescript blnk.Search.getReindexStatus wrap {1}
const response = await blnk.Search.getReindexStatus();
```

This method takes no arguments.

---

## Response shape

```json 200 OK wrap
{
  "status": "in_progress",
  "phase": "indexing_transactions",
  "total_records": 2145709,
  "processed_records": 2145709,
  "started_at": "2026-02-12T12:13:10.448612096Z"
}
```

| Field | Description |
| :-- | :-- |
| `status` | Current state, `in_progress` or `completed`. |
| `phase` | Current phase, for example `indexing_transactions`, or `done` when complete. |
| `total_records` | Total number of records to index. |
| `processed_records` | Number of records processed so far. |
| `started_at` | Timestamp when the reindex began. |
| `completed_at` | Timestamp when the reindex finished. Only present once `status` is `completed`. |
| `errors` | Array of error messages. Only present when an error occurs. |

---

## Related docs

<CardGroup cols={2}>
<Card title="How reindexing works" icon="rotate" href="/search/typesense/reindex">
When and why to rebuild the index.
</Card>

<Card title="Check reindex progress" icon="book-open" href="/reference/get-reindex">
HTTP request and response schema.
</Card>
</CardGroup>

---

<NeedHelp />
